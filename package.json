{
  "name": "tma-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --parallel -r dev",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean"
  },
  "dependencies": {
    "@types/node": "^22.13.9",
    "typescript": "^5.8.2",
    "jest": "^29.7.0"
  }
}
